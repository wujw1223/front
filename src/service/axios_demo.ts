// httpbin.org

// axois.interceptors.request(response).use(fn1请求发送成功, fn2请求发送失败)
// fn1 = config => { reutnr config }

// proccess.env.NODE_ENV 是根据definePlugin
// 在vue cli中 可以通过 .env.development .env.production .env.test
// BASE_URL VUE_BASE开头
console.log('---')
